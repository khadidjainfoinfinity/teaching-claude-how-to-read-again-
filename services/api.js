/**
 * This file previously contained a browser-side axios client (using localStorage).
 * It does not belong in a Node.js backend.
 *
 * HTTP communication is handled server-side in each route/controller.
 * Email delivery lives in services/emailService.js.
 */
