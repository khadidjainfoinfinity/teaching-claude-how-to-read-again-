/**
 * Superseded by utils/tokenUtils.js which provides signToken() with a richer
 * payload (id, email, role) and customerPayload() for API responses.
 */
export { signToken as default } from './tokenUtils.js';
